# One Azure App Registration for the whole product (users never enter this).
# After setting, restart: npm start
# Users only click Settings → Sign in with Microsoft.

MS_CLIENT_ID=
# MS_TENANT_ID=common
# MS_CLIENT_SECRET=
